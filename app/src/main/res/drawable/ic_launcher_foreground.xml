<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Fundo transparente — o background já é preto -->

    <!-- Círculo amarelo DENTRO da safe zone real do adaptive icon: só o
         círculo central de 66dp (de 108) é garantido — qualquer coisa maior
         é cortada pela máscara do launcher (círculo/squircle), o que deixava
         o ícone "quebrado". Diâmetro 62 < 66. -->
    <path
        android:fillColor="#FFC107"
        android:pathData="M54,23a31,31 0 1,0 0.001,0Z"/>

    <!-- W branco em traço único (stroke): nunca se distorce -->
    <path
        android:strokeColor="#FFFFFF"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M35,42 L44.5,70 L54,51.5 L63.5,70 L73,42"/>

</vector>
